public class Solution{
   /* String[] used_id = {"frodo","fradi","crodo","abc123","frodoc"};
    String[] banned_id = {"fr*d*" , "abc1**"};
    int result = 0;

    for( int i = 0; i < used_id.length ; i++){

        for(int j = 0; j < banned_id.length; j++){
            int count = 0;
            if(used_id[i].length == banned_id[j].length() ){

                for(int k = 0; k < used_id[i].length(); k++){
                    if(banned_id[j].charAt(k) == '*') count++;
                    else if(used_id[i].charAt(k) == banned_id[j].charAt(k)) count++;
                }
                if(count == used_id[i].length()) {
                    result++;
                    break;
                }
            }
        }

    } */

}
